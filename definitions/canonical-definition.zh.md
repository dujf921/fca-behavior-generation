# 标准定义 | 人类行为 FCA

人类行为 FCA（Functional Causal Architecture）是用于解释人类行为如何生成、稳定、断裂、锁死，并被现实反馈更新的功能因果架构。

其分析对象是行为生成链，而不是人格分类，不是心理标签，不是动机理论，也不是聊天壳。

可观察行为输出是分析入口，不是最终因果结论。

人类行为 FCA 的目标是定位生成、维持与更新环节中的功能断裂点，而不是给人贴标签。
